# LightHouse configuration — mesh second Intermediate Authority (sibling
# of mesh-ia, subordinate of mesh-ta). Exists to give mesh-leaf-multi a
# second parent for multi-parent / multiple-valid-chains testing.
entity_id: "http://mesh-ia2:8080"

server:
  port: 8080

storage:
  driver: sqlite
  data_dir: /data

signing:
  kms: filesystem
  auto_generate_keys: true
  filesystem:
    key_dir: /data/keys

api:
  admin:
    enabled: true
    users_enabled: true
    actor_source: "header"
    actor_header: "X-Gateway-User-Email"

endpoints:
  fetch:
    path: /fetch
  list:
    path: /list
  resolve:
    path: /resolve
  trust_mark:
    path: /trust_mark
  trust_mark_status:
    path: /trust_mark/status
  trust_mark_list:
    path: /trust_mark/list
  historical_keys:
    path: /historical_keys

stats:
  enabled: true
